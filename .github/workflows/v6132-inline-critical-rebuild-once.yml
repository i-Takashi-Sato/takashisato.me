name: One-shot v6.13.2 inline-critical rebuild

on:
  push:
    branches:
      - design/editorial-refinement-v613

permissions:
  contents: write

jobs:
  rebuild:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: design/editorial-refinement-v613
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.13'
      - run: python -m pip install --requirement tools/requirements.txt
      - name: Rebuild and verify
        run: |
          python tools/build_site.py
          python tools/generate_og.py
          python tools/verify_v62.py
          python -m compileall -q tools
          node --check assets/site.js
          grep -R --line-number '<style data-critical>' index.html papers/index.html papers/part1.html papers/part2.html papers/part3.html about.html
          grep -R --line-number 'site.css?v=6.13.2" media="print" data-full-style' index.html papers/index.html papers/part1.html papers/part2.html papers/part3.html about.html
          ! grep -R --line-number -E 'content:[[:space:]]*"(序|破|急)"|Jo / Ha / Kyu' assets/site.css assets/critical.css
      - name: Commit output
        run: |
          rm -f .github/workflows/v6132-inline-critical-rebuild-once.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Rebuild v6.13.2 with inline critical CSS'
          git push origin HEAD:design/editorial-refinement-v613
